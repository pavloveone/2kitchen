services:
  - type: web
    name: 2kitchen-backend
    env: go
    region: frankfurt
    plan: free
    rootDir: backend
    buildCommand: go build -o main ./cmd
    startCommand: ./main
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        sync: false
